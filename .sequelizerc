const path = require("path");

module.exports = {
  config: path.resolve("dist", "infra", "config", "db.js"),
  "migrations-path": path.resolve("dist", "database", "migrations"),
  "seeders-path": path.resolve("dist", "database", "seeders"),
};
